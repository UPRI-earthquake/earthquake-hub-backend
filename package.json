{
  "name": "back-end",
  "version": "1.0.0",
  "description": "This repo contains the backend code for UPRI earthquake monitoring system",
  "main": "index.js",
  "scripts": {
    "start:dev": "nodemon index.js",
    "start:dev:help": "echo 'Runs nodemon on index.js. To be used inside docker...",

    "test:locally": "jest --setupFiles dotenv/config",
    "test:locally:help": "echo 'Runs tests against Jest, and expects env-vars to come from a local .env file. To be used locally...'",

    "start:dev:locally": "nodemon -r dotenv/config index.js",
    "start:dev:locally:help": "echo 'Runs nodemon on index.js, and expects env-vars to come from a local .env file. To be used locally...'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/prokorpio/UPRI-DC-back-end.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/prokorpio/UPRI-DC-back-end/issues"
  },
  "homepage": "https://github.com/prokorpio/UPRI-DC-back-end#readme",
  "dependencies": {
    "axios": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^7.0.0",
    "mysql2": "^3.2.0",
    "redis": "^4.6.5",
    "web-push": "^3.4.5"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "nodemon": "^2.0.21",
    "dotenv": "^16.0.0",
    "supertest": "^6.3.3",
    "testcontainers": "^9.1.3"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
